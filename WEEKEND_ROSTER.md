# 맥가이 토·일 명단 반영

사용자가 이 작업에서 **로그인했어**라고 하면 아래 절차를 실행한다. 별도 정기 예약은 만들지 않는다. 맥가이는 로그인된 브라우저 UI로만 읽으며 API나 저장된 세션을 추출하지 않는다.

## 조회와 반영

1. 실제 맥가이 로그인과 기관 `서로국어논술 영천관`, 담당 `김이내`를 확인한다.
2. 수업관리 → 수업목록에서 한국 시간 기준 요청한 주차를 선택한다. 기본은 이번 주 토·일이며, 토요일·일요일 수업이 끝난 후 다음 주를 요청하면 해당 주차를 사용한다.
3. 해당 날짜의 수업 시간, 반, 학생 수를 읽고 **학생 수 셀**을 클릭한다. 펼쳐진 iframe의 학생명·학교를 읽는다. 수업 화면이 월별 재원 명단보다 우선한다. 전반으로 생긴 날짜별 차이를 기본 반 배정에 쓰지 않는다.
4. 토요일 한백 10:30·14:30, 치동 18:00 / 일요일 한백 10:30·치동 14:30을 모두 확인한다. 추가 수업, 변경 시간, 목록 누락은 임의 대응하지 않고 사용자에게 알린다. 0명인 수업도 실제 0명 표시를 확인해서 명시한다. 모든 수업의 표시 인원과 추출 인원은 같아야 한다.
5. 매 실행 새 국어왕 학생 목록을 읽고 이름(숫자 접미사 포함)·학교가 정확히 일치하는 활성 학생 ID로 연결한다. 이름이 없거나 여러 명이면 반영을 중지한다. 이름만 보고 계정을 신설하거나 휴퇴원 상태를 변경하지 않는다.
6. 비공개 로컬 `work/`에 원본, 확인 시각, 국어왕 대조 자료를 보관한다. 공개 Git 저장소에는 학생명·ID·명단 파일을 넣지 않는다.
7. `node scripts/prepare-weekend-roster.cjs SOURCE_JSON USERS_JSON OUTPUT_DIR OBSERVED_AT_ISO`로 두 날짜의 원자적 반영 SQL을 만든다. 원본 각 행은 `{date,class_id,expected_count,school,names}` 형식이며 names는 공백으로 구분된 이름이다. 사용자 데이터에는 id,name,school,role,status가 필요하다.
8. 출결 프로젝트 `vpucowvjgkppdbotxjxd`의 로그인된 Supabase SQL Editor에서 생성 SQL을 실행한다. Monaco 편집기는 `fill` 대신 편집창에서 전체 선택 후 붙여넣기를 사용하고, 전체 선택·복사로 원본과 일치함을 확인한 뒤 Run selected로 실행한다. 초기 서버 구조는 `weekend-roster.sql`. 서버 로그인 만료라면 재로그인을 요청한다. 실제 확인하지 않은 오래된 명단을 재사용하지 않는다.
9. `read_weekend_roster`로 두 날짜를 다시 읽어 원본의 ID·반·인원을 전부 대조한다. 바뀐 학생을 추가·제외·전반으로 나누어 보고한다. 조회 또는 저장 실패를 빈 명단으로 취급하지 않는다.
10. 배포된 패드와 출결 현황판에서 해당 날짜의 명단 버전·인원을 확인한다. 물리적 패드 화면을 직접 확인하지 못하면 이를 구분해서 말한다.

## 보존 및 동작

- 명단은 날짜별 최신 버전을 사용한다. 같은 주에도 재조회·재반영 가능하다.
- 버전 저장은 추가 방식이다. 이전 명단과 실제 출석은 삭제하지 않는다.
- 같은 데이터 재시도는 같은 버전을 반환한다. 더 오래된 조회 결과가 최신 결과를 덮어쓰지 못한다.
- 수업 체크인은 입력 때 최신 날짜별 명단을 조회한다. 아직 반영되지 않은 주말, 조회 실패, 명단 미포함, 같은 날 두 수업은 임의 출석 처리하지 않는다.
- 등록된 기본 반은 바꾸지 않는다. 클리닉과 수요일 수업 처리는 기존 흐름을 사용한다.
- 학생 이름·연락처는 새 조회 함수에서 반환하지 않는다. 패드는 기존 국어왕 학생 ID로 연결한다. 쓰기는 관리자 DB 세션으로만 가능하다.
- 출석 현황의 명단 변경 전 출석은 그대로 남고, 교사가 취소할 수 있다.
- 패드 최초 업데이트는 새로고침이 필요하다. 이후 출석 입력마다 최신 명단을 조회하므로 별도 명단 파일 업로드가 필요 없다.

## 참고

Microsoft Playwright의 요소 기반 브라우저 조작과 검증 방식을 참고한다: https://github.com/microsoft/playwright
Ajv의 구조 검증 방법도 검토했으나, 현재 데이터 형태는 단순하므로 기존 JavaScript·PostgreSQL 검증을 사용해 새 런타임 의존성을 추가하지 않았다: https://github.com/ajv-validator/ajv

추가로 권장하는 운영 개선: 수업 시작 직전 한 번 더 반영, 누락·동명이인만 사용자에게 질문, 변경내역 보고와 이전 버전 복구, 조회 실패 시 기존 기록 보존. 문자 발송은 별도 요청 범위다.
